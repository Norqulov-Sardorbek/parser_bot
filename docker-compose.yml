services:
  web:
    build: .
    command: >
      sh -c "
      python manage.py migrate &&
      python manage.py runserver 0.0.0.0:8070 
      "
    env_file:
      - .env
    ports:
      - "8070:8070"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: always
